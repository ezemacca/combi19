<h2> Confirmar Pasaje <h2>

<div>
<%= @viaje.fecha.strftime("Fecha de salida: %d/%m/%y") %><br >
<%= @viaje.fecha.strftime("Hora de salida: %H:%M") %><br >
<%= @viaje.fecha_llegada.strftime("Hora aproximada de llegada: %H:%M") %><br >
Origen: <%= @origen.ciudad.titleize %><br >
Destrino: <%= @destino.ciudad.titleize %><br >
Por: <%= @ruta.nombre.titleize%><br >
Tipo de combi: <%= @combi.clasificacion %><br>
Precio: $$$<%$$%> <br >

</div>

<div>
<h2>Quiere comprar algun Producto/Vianda para su viaje?<h2>
Lista de Productos
 <table class="table">
    <thead>
      <tr>
        <th scope="col">Nombre</th>
        <th scope="col">Descripcion</th>
        <th scope="col">Precio</th>
        <th scope="col">Cantidad</th>
        <th scope="col">Acciones</th>
      </tr>
    </thead>
    <tbody>

  <tbody>
    <% @producto.each do |producto| %>
          <tr>
            <td><%= producto.nombre.titleize %></td>
            <td><%= producto.descripcion %></td>
            <td>$<%= producto.precio %> </td>
            <% if @cantidad.find_by(producto_id: producto.id) != nil %>
              <td><%= @cantidad.find_by(producto_id: producto.id).cantidad %></td>
            <% else %>
              <td>0</td>
            <% end %>
            <td><%= link_to "Agregar producto", agregarproducto_usuario_path(:producto => producto, :viaje => @viaje, :pasaje => @pasaje), method: :patch%>
            <%= link_to "Cancelar producto", cancelarproducto_usuario_path(:producto => producto,:pasaje => @pasaje,:viaje => @viaje), method: :delete %> <br /> </td>
          </tr>	
    <% end %>
  </tbody>
</table>
<div> 
<%= link_to "Volver", usuario_index_path %>
<%= link_to "Confirmar compra", confirmarcompra_usuario_path(:pasaje => @pasaje) %>
</div>