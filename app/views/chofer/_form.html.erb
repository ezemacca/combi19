<h2>Registrar chofer</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true, placeholder: "Email" %>
  </div>

  <div class="field">
    <%= f.label "Contraseña" %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> caracteres minimos)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password", required: true, placeholder: "Contraseña" %>
  </div>

  <div class="field">
    <%= f.label "Confirme la contraseña" %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", required: true, placeholder: "Confirmar contraseña" %>
  </div>

    <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre, required: true, placeholder: 'Ingrese el nombre del chofer' %>
    </div>

    <div class="field">
    <%= f.label :apellido %><br />
    <%= f.text_field :apellido, required: true, placeholder: 'Ingrese el apellido del chofer' %>
    </div>

    <div class="field">
    <%= f.label :DNI %><br />
    <%= f.text_field :DNI, required: true, placeholder: 'Ingrese el DNI del chofer' %>
    </div>

    <div class="field">
    <%= f.label "Numero de contacto" %><br />
    <%= f.text_field :telefono, required: true, placeholder: 'Ingrese el numero de contacto del chofer' %>
    </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>

<% end %>

<%= render "devise/shared/links" %>
