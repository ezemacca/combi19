


<%= form_with(model: viaje, local: true) do |form| %>
  <% if viaje.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(viaje.errors.count, "error") %> prohibited this viaje from being saved:</h2>

      <ul>
        <% viaje.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha %><br />
    <%= form.datetime_field :fecha, start_year: 2020, end_year: 2030, required: true %>
  </div>

<!--  <div class="field">
    <%= form.label :origen %>
    <%= form.collection_select :origen, Lugar.where(eliminado: false), :id, :mostrar_atributos, { include_blank: "" }, required:true %>
  </div>

  <div class="field">
    <%= form.label :destino %>
    <%= form.collection_select :destino, Lugar.where(eliminado: false), :id, :mostrar_atributos, { include_blank: "" }, required:true %>
  </div>-->

  <div class="field">
    <%= form.label :ruta %>
    <%= form.collection_select :ruta, Rutum.where(eliminado: false), :id, :mostrar_nombre, { include_blank: "" }, required:true %>
  </div>

  <div class="field">
    <%= form.label :combi %>
    <%= form.collection_select :combi, Combi.where(eliminado: false), :id, :mostrar_atributos, { include_blank: "" }, required:true %>
  </div>

  <div class="field">
    <%= form.label :chofer %>
    <%= form.collection_select :chofer, Usuario.where(role: "chofer", eliminado: false), :id, :mostrar_atributos, { include_blank: "" },  required: true %>
  </div>

  <div class="field">
    <%= form.label :fecha_llegada %>
    <%= form.datetime_field :fecha_llegada, start_year: 2020, end_year: 2030 %>
  </div>



  <div class="actions">
    <%= form.submit "Agregar viaje" %>
  </div>
<% end %>
