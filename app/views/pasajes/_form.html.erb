<%= form_with(model: pasaje, local: true) do |form| %>
  <% if pasaje.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pasaje.errors.count, "error") %> prohibited this pasaje from being saved:</h2>

      <ul>
        <% pasaje.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :precio %>
    <%= form.text_field :precio %>
  </div>

  <div class="field">
    <%= form.label : %>
    <%= form.number_field : %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
